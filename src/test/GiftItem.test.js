import "@testing-library/jest-dom";

describe('Pruebas en el Componente GifItem', () => { 
    
    test('Prueba que se renderice correctamente', () => { 
        /* utilizaremos la libreria enzyme, la cual ademas de hacer las pruebas en componentes mas facilmente,
         nos genera capturas snapshoot de la estructura del componente y la compara con el resultado esperado
        
        */

         

     })
 })